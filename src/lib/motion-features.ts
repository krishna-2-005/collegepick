// Loaded lazily by <LazyMotion> so animation code stays out of the first bundle.
// Every animation in the app runs after a user action, never on first paint.
export { domAnimation as default } from "framer-motion";
